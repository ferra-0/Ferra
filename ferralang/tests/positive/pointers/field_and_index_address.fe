stct Pair { left: i64, right: i32 }

fn main(): i64 {
  let pair: Pair
  let values: i64[] = [1, 2]
  let field: ptr = ^(pair.right)
  let element: ptr = ^(values[1])
  field^ = 40
  element^ += 40
  if pair.right == 40 and values[1] == 42 { ret 0 }
  ret 1
}
