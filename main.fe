take "fe/math.fe"
// importing with 'take' keyword
// takes files from ferra's root directory

// to import std files use fe/ directory

ftake "./some.fe"
// importing with 'take' keyword
// takes files from current working directory

func main() {
  log(round(0.5)) // 1

  some() // Hello!
}