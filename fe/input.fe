/*
    ____                  __ 
   /  _/___  ____  __  __/ /_
   / // __ \/ __ \/ / / / __/
 _/ // / / / /_/ / /_/ / /_  
/___/_/ /_/ .___/\__,_/\__/  
         /_/                 
Ferra standart library

*/

__llh "declare i32 @scanf(ptr, ptr)"
extern func fgets(ptr, i32, ptr): ptr
__llh "@stdin = external global ptr"
__llh "declare i32 @getchar()"

take "fe/string.fe"

func ini64(): i64 {
  var x: i64 = 0

  scanf("%d", ^x)

  ret x
}
func inf64(): f64 {
  var x: f64 = 0.0

  scanf("%lf", ^x)

  ret x
}
func instr(s: i64): str {
  var buf: i8* = malloc(s)
  if fgets(buf, s, stdin) is null {
    buf^ = 0
    ret buf
  }

  var n: usize = strlen(buf)
  if n > 0 and buf[n - 1] is 10 {
    var end: i8* = buf + (n - 1)
    end^ = 0
    n = n - 1
  }
  if n > 0 and buf[n - 1] is 13 {
    var carriage: i8* = buf + (n - 1)
    carriage^ = 0
  }

  ret buf
}
func inchar(): i32{
  ret getchar()
}
